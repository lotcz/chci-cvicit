Installation:

cd /var/www/
mkdir chci-cvicit
git clone https://github.com/lotcz/chci-cvicit.git chci-cvicit
sudo make install

Update:

cd /var/www/chci-cvicit
sudo make update

export to PDF:

- open in Chrome
- add ?print-pdf to URL
- click Print
- open settings and enable background